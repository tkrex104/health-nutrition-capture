# Chappyへ渡す設定依頼

以下をそのままChappyへ渡してください。

---

このNutrition Screenshot Capture Kitを自分用に設定したいです。私はiPhoneを使っており、栄養アプリのスクリーンショットを共有シートから送信したいです。

最初に、保存先として何を使うかを質問してください。私はコードに詳しくないため、一Stepずつ設定し、その都度テストしてください。

必ず守ってください。

1. API key、認証token、Web App URL、Spreadsheet IDをソースコードへ直接書かないでください。Apps Scriptのスクリプトプロパティなど安全な設定場所を使ってください。
2. 私の実スクリーンショット、実endpoint、個人の栄養データを公開場所やGitHubへアップロードしないでください。
3. iPhone Shortcutは、共有シートから画像を受信し、横1200pxへResizeしてBase64化し、`today`か`yesterday`を選んで送る構成にしてください。
4. BackendはJSONを返し、失敗時はエラーを表示して原因を切り分けてください。
5. 最初はSynthetic Sampleで、保存・必須値検証・エラー表示を確認してください。
6. 実画像のテストは、endpointと保存先を確認してから一枚ずつ行ってください。
7. 同日の再送では新しい行を増やさず、同日の保存内容を更新してください。

コードの内容を前提に説明せず、次に押す画面や入力する設定値を短く説明してください。失敗した場合は、表示されたエラー、Shortcutのrequest、Backendの実行ログの順で確認してください。

---
