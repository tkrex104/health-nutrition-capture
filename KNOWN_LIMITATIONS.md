# Known limitations

| 項目 | 実装上の扱い |
|---|---|
| あすけん画面変更 | 未対応。画像内の摂取量表示や配置が変われば抽出精度へ影響します。 |
| 画像抽出誤り | JSON schemaと数値・非負値検証は行いますが、画像の値が正しい保証はありません。保存前にResponseを確認できます。 |
| 栄養項目の欠損 | スクリーンショット入力では17栄養値すべてが必須です。欠損時は保存しません。 |
| 今日／昨日選択 | `date_mode`は`today`か`yesterday`です。選択ミスの自動検出はありません。 |
| 同日再送 | 同じ日付の既存行を更新し、新しい重複行を作らない実装です。 |
| Endpoint設定 | Web App URL、token、Script Propertiesの設定不備はJSONエラーとして返ります。 |
| ネットワーク・API障害 | OpenAIまたはWeb Appへの通信に失敗すると保存されず、JSONエラーを返します。自動再送は実装していません。 |
